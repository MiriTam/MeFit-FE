import { Box, Typography } from '@mui/material';

const WorkoutFormsList = () => {
	return (
		<Box className=''>
			<Typography>WorkoutFormsList</Typography>
		</Box>
	);
};

export default WorkoutFormsList;
