import { Box, Typography } from '@mui/material';

const ProgramFormsList = () => {
	return (
		<Box className=''>
			<Typography>ProgramFormsList</Typography>
		</Box>
	);
};

export default ProgramFormsList;
