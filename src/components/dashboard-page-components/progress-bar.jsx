import { Typography } from "@mui/material"

const ProgressBar = () => {

    return (
        <Typography>progress bar</Typography>
    )
}

export default ProgressBar;
// Export progress bar
// from material UI?